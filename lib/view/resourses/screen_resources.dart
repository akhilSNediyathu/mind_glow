import 'package:flutter/material.dart';

class ScreenResources extends StatelessWidget {
  const ScreenResources({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold
    (
      body: 
      Center(
            child: Text('Screen Resources'),
      ),
    );
  }
}