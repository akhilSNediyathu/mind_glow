import 'package:flutter/material.dart';

class ScreenWellnessPlanner extends StatelessWidget {
  const ScreenWellnessPlanner({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
            child: Text('welness planner'),
      ),
    );
  }
}