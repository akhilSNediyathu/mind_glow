import 'package:flutter/material.dart';

class ScreenMoodTracker extends StatelessWidget {
  const ScreenMoodTracker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
            child: Text('mood tracker'),
      ),
    );
  }
}